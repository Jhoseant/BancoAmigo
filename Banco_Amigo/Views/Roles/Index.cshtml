@model IEnumerable<Banco_Amigo.Models.ba_roles>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ro_rol)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ro_estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ro_rol)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ro_estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ro_idrol }) |
            @Html.ActionLink("Details", "Details", new { id=item.ro_idrol }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ro_idrol })
        </td>
    </tr>
}

</table>
